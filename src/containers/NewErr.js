import React, { PureComponent } from 'react';

export default class NewErr extends PureComponent {
  // static propTypes = {
  // };

  // state = {
  // };

  render() {

    return (
      <>
        <h1>New Err Babbbieeee</h1>
      </>
    );
  }
}
